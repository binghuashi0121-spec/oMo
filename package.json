{
  "scripts": {
    "admin:web:dev": "npm --prefix omo-admin-web run dev",
    "admin:web:build": "npm --prefix omo-admin-web run build",
    "admin:web:test": "npm --prefix omo-admin-web run test",
    "admin:api:dev": "npm --prefix omo-admin-api run start:dev",
    "admin:api:build": "npm --prefix omo-admin-api run build",
    "admin:api:test": "npm --prefix omo-admin-api run test",
    "admin:check": "npm --prefix omo-admin-web run check && npm --prefix omo-admin-api run check",
    "mini:config:test": "node --test omo-mini-program/tests/environment-config.test.js",
    "mini:test": "node --test omo-mini-program/tests/*.test.js",
    "staging:check": "node scripts/staging/check-config.js",
    "staging:test": "node --test scripts/staging/*.test.js",
    "cloudbase:check-env": "node scripts/cloudbase/check-env.js",
    "cloudbase:backup:inspect": "node scripts/cloudbase/inspect-backup.js",
    "cloudbase:backup:validate": "node scripts/cloudbase/validate-import.js",
    "mqtt-bridge:ensure": "node scripts/mqtt-bridge/ensure-running.js",
    "mqtt-bridge:stop": "node scripts/mqtt-bridge/stop.js",
    "real-device:prepare": "node scripts/real-device/prepare.js"
  },
  "dependencies": {}
}
